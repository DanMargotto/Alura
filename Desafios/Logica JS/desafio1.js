// Resolução de 10 desafios

1. let welcomeMessage = 'Boas vindas ao nosso site!';
   //Variáveis de entrada e saída do usuário.
   alert (welcomeMessage);

2. let name = 'Lua';
3. let age = 25;
4. let salesNumber = 50;
5. let availableBalance = 1000;

6. alert ('Erro! Preencha todos os campos');
7. let errorMessage = 'Erro! Preencha todos os campos'
   alert (errorMessage);

8. let name = prompt('Digite o seu nome: '); //Pede para o usuario digitar
9. let age = prompt('Qual é a sua idade?');  //Pedir informações ao usuario

10. let age = prompt('Qual é a sua idade?');
    if(age >= 18)
    {
        alert ('Pode tirar a habilitação!');
    }
    else
    {
        alert ("Você não pode tirar a habilitação!");
    }